struct Coordinate {
  int x;
  int y;
  int z;
};

struct Coordinate new_coord(int x, int y, int z) {
  struct Coordinate coo = {
    .x = x,
    .y = y,
    .z = z
  };
  return coo;
}

struct Coordinate new_coord(int x, int y, int z) {
  struct Coordinate coo = {
    .x = x,
    .y = y,
    .z = z
  };
  return coo;
}
